# 任务清单：UI 布局快照 HTML

目录：`helloagents/plan/202602161858_ui_layout_snapshots/`

---

## 1. 前台 UI 快照
- [ ] 1.1 基于 `apps/web/public/_templates/articles.html` 生成 `docs/ui-snapshots/web-articles.html`，
  使用示例数据；验证 `why.md#需求-静态布局快照-场景前台布局查看`。
- [ ] 1.2 基于 `apps/web/public/_templates/post.html` 生成 `docs/ui-snapshots/web-post.html`，
  使用示例数据；验证 `why.md#需求-静态布局快照-场景前台布局查看`。

## 2. 后台 UI 快照
- [ ] 2.1 生成 `docs/ui-snapshots/admin-login.html`，布局对齐 `LoginPage`；验证
  `why.md#需求-静态布局快照-场景后台布局查看`。
- [ ] 2.2 生成 `docs/ui-snapshots/admin-posts.html`，布局对齐 `PostsPage`；验证
  `why.md#需求-静态布局快照-场景后台布局查看`。
- [ ] 2.3 生成 `docs/ui-snapshots/admin-editor.html`，布局对齐 `EditorPage`；验证
  `why.md#需求-静态布局快照-场景后台布局查看`。
- [ ] 2.4 生成 `docs/ui-snapshots/admin-settings.html`，布局对齐 `SettingsPage`；验证
  `why.md#需求-静态布局快照-场景后台布局查看`。
- [ ] 2.5 生成 `docs/ui-snapshots/admin-account.html`，布局对齐 `AccountPage`；验证
  `why.md#需求-静态布局快照-场景后台布局查看`。

## 3. 安全检查
- [ ] 3.1 清理快照中的账号、Token、生产 URL；验证
  `why.md#需求-静态布局快照-场景后台布局查看`。

## 4. 文档更新
- [ ] 4.1 新增 `docs/ui-snapshots/README.md`，包含预览步骤与文件清单；验证
  `why.md#需求-静态布局快照`。

## 5. 测试
- [ ] 5.1 手动打开每个快照 HTML，确认布局结构与当前 UI 层级一致。
