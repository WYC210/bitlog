# 开发者工具 client_code

复制下面每个工具的代码块内容，粘贴到后台对应工具的 client_code 字段。
每个代码块默认是单行，直接复制整行即可（现在也支持多行存储与执行）。

---

## 1. base64

```
var r=document.getElementById('tool-root');var mode='encode',inp='',out='';var s=document.createElement('style');s.textContent='#b64{max-width:800px;margin:0 auto;padding:12px;font-family:system-ui,sans-serif}#b64 *{box-sizing:border-box}.b64-bar{display:flex;justify-content:space-between;background:#1e293b;padding:3px 6px;border-radius:6px 6px 0 0;margin-bottom:0}.b64-tab{padding:4px 12px;border:none;border-radius:4px;cursor:pointer;font-size:.8rem;background:transparent;color:#94a3b8}.b64-tab.on{background:#3b82f6;color:#fff}.b64-areas{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:8px}.b64-areas textarea{width:100%;height:200px;padding:8px;font-family:monospace;font-size:.8rem;background:#0f172a;color:#e2e8f0;border:1px solid #334155;border-radius:4px;resize:vertical}@media(max-width:600px){.b64-areas{grid-template-columns:1fr}}';document.head.appendChild(s);function convert(t){if(!t.trim())return'';try{return mode==='encode'?btoa(unescape(encodeURIComponent(t))):decodeURIComponent(escape(atob(t)));}catch(e){return mode==='encode'?'编码失败':'解码失败：无效的Base64字符串';}}function render(){r.innerHTML='<div id="b64"><div class="b64-bar"><div><button class="b64-tab'+(mode==='encode'?' on':'')+'" id="b64e">编码</button><button class="b64-tab'+(mode==='decode'?' on':'')+'" id="b64d">解码</button></div><button class="b64-tab" id="b64c">× 清空</button></div><div class="b64-areas"><textarea id="b64i" placeholder="'+(mode==='encode'?'输入需要编码的文本...':'输入需要解码的Base64字符串...')+'" spellcheck="false">'+inp+'</textarea><textarea id="b64o" placeholder="'+(mode==='encode'?'Base64编码结果...':'解码后的文本...')+'" readonly spellcheck="false">'+out+'</textarea></div></div>';document.getElementById('b64e').onclick=function(){mode='encode';out=convert(inp);render();};document.getElementById('b64d').onclick=function(){mode='decode';out=convert(inp);render();};document.getElementById('b64c').onclick=function(){inp='';out='';render();};document.getElementById('b64i').oninput=function(){inp=this.value;out=convert(inp);document.getElementById('b64o').value=out;};}render();
```

---

## 2. timestamp

```
var r=document.getElementById('tool-root');var ts='',dt='';var s=document.createElement('style');s.textContent='#tsc{max-width:600px;margin:0 auto;padding:12px;font-family:system-ui,sans-serif;color:#e2e8f0}#tsc *{box-sizing:border-box}.tsc-bar{display:flex;justify-content:space-between;background:#1e293b;padding:4px 8px;border-radius:6px;margin-bottom:12px}.tsc-btn{padding:4px 10px;border:none;border-radius:4px;cursor:pointer;font-size:.8rem;background:#334155;color:#94a3b8}.tsc-btn:hover{background:#475569;color:#e2e8f0}.tsc-row{display:flex;align-items:center;gap:8px;margin-bottom:8px}.tsc-label{min-width:80px;font-size:.8rem;color:#94a3b8}.tsc-input{flex:1;padding:6px 8px;font-family:monospace;font-size:.8rem;background:#0f172a;color:#e2e8f0;border:1px solid #334155;border-radius:4px}.tsc-input.ro{background:#1e293b;color:#64748b;cursor:default}';document.head.appendChild(s);function pad(n){return String(n).padStart(2,'0');}function fmtDate(ms){var d=new Date(ms);return d.getFullYear()+'-'+pad(d.getMonth()+1)+'-'+pad(d.getDate())+' '+pad(d.getHours())+':'+pad(d.getMinutes())+':'+pad(d.getSeconds());}function fmtUTC(ms){var d=new Date(ms);return d.getUTCFullYear()+'-'+pad(d.getUTCMonth()+1)+'-'+pad(d.getUTCDate())+' '+pad(d.getUTCHours())+':'+pad(d.getUTCMinutes())+':'+pad(d.getUTCSeconds())+' UTC';}function relTime(ms){var diff=Date.now()-ms;var abs=Math.abs(diff);var future=diff<0;var sec=Math.floor(abs/1000);if(sec<60)return sec+'秒'+(future?'后':'前');var m=Math.floor(sec/60);if(m<60)return m+'分钟'+(future?'后':'前');var h=Math.floor(m/60);if(h<24)return h+'小时'+(future?'后':'前');return Math.floor(h/24)+'天'+(future?'后':'前');}function tsToMs(v){var n=parseInt(v);return v.length<=10?n*1000:n;}function render(){var ms=ts?tsToMs(ts):null;var utc=ms?fmtUTC(ms):'-';var local=ms?fmtDate(ms):'-';var rel=ms?relTime(ms):'-';r.innerHTML='<div id="tsc"><div class="tsc-bar"><button class="tsc-btn" id="tsc-now">当前时间</button><button class="tsc-btn" id="tsc-clr">× 清空</button></div><div class="tsc-row"><span class="tsc-label">时间戳</span><input class="tsc-input" id="tsc-ts" value="'+ts+'" placeholder="输入时间戳 (秒或毫秒)..."></div><div class="tsc-row"><span class="tsc-label">日期时间</span><input class="tsc-input" id="tsc-dt" value="'+dt+'" placeholder="2025-01-15 14:30:00"></div><div class="tsc-row"><span class="tsc-label">UTC时间</span><input class="tsc-input ro" value="'+utc+'" readonly></div><div class="tsc-row"><span class="tsc-label">本地时间</span><input class="tsc-input ro" value="'+local+'" readonly></div><div class="tsc-row"><span class="tsc-label">相对时间</span><input class="tsc-input ro" value="'+rel+'" readonly></div></div>';document.getElementById('tsc-now').onclick=function(){ts=String(Math.floor(Date.now()/1000));dt=fmtDate(Date.now());render();};document.getElementById('tsc-clr').onclick=function(){ts='';dt='';render();};document.getElementById('tsc-ts').oninput=function(){ts=this.value;var n=parseInt(ts);if(!isNaN(n)){var ms2=tsToMs(ts);dt=fmtDate(ms2);}render();};document.getElementById('tsc-dt').oninput=function(){dt=this.value;var d=new Date(dt);if(!isNaN(d.getTime())){ts=String(Math.floor(d.getTime()/1000));}render();};}render();
```

---

## 3. jwt-decoder

```
var r=document.getElementById('tool-root');var tok='',result=null,err='';var s=document.createElement('style');s.textContent='#jwt{max-width:800px;margin:0 auto;padding:12px;font-family:system-ui,sans-serif}#jwt *{box-sizing:border-box}.jwt-bar{display:flex;justify-content:space-between;background:#1e293b;padding:3px 6px;border-radius:6px;margin-bottom:8px}.jwt-btn{padding:4px 10px;border:none;border-radius:4px;cursor:pointer;font-size:.8rem;background:#334155;color:#94a3b8}.jwt-btn:hover{background:#475569;color:#e2e8f0}.jwt-areas{display:grid;grid-template-columns:1fr 1fr;gap:8px}.jwt-areas textarea{width:100%;height:180px;padding:8px;font-family:monospace;font-size:.75rem;background:#0f172a;color:#e2e8f0;border:1px solid #334155;border-radius:4px;resize:vertical}.jwt-err{padding:10px;background:#450a0a;color:#fca5a5;border-radius:4px;font-size:.8rem;margin-top:8px}.jwt-pre{padding:8px;background:#0f172a;color:#e2e8f0;border:1px solid #334155;border-radius:4px;font-family:monospace;font-size:.75rem;white-space:pre-wrap;word-break:break-all;min-height:180px}@media(max-width:600px){.jwt-areas{grid-template-columns:1fr}}';document.head.appendChild(s);var SAMPLE='eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiaWF0IjoxNTE2MjM5MDIyfQ.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c';function decode(t){err='';result=null;if(!t.trim())return;var p=t.trim().split('.');if(p.length!==3){err='无效的JWT格式，应包含3个部分（header.payload.signature）';return;}try{var h=JSON.parse(atob(p[0].replace(/-/g,'+').replace(/_/g,'/')));var pl=JSON.parse(atob(p[1].replace(/-/g,'+').replace(/_/g,'/')));result={header:h,payload:pl,signature:p[2]};}catch(e){err='JWT解析失败：'+e.message;}}var timer=null;function render(){var outHtml=err?'<div class="jwt-err">'+err+'</div>':result?'<pre class="jwt-pre">'+JSON.stringify(result,null,2)+'</pre>':'<div class="jwt-pre" style="color:#475569;display:flex;align-items:center;justify-content:center;font-size:2rem">🔍</div>';r.innerHTML='<div id="jwt"><div class="jwt-bar"><button class="jwt-btn" id="jwt-sample">示例</button><button class="jwt-btn" id="jwt-clr">× 清空</button></div><div class="jwt-areas"><textarea id="jwt-in" placeholder="粘贴JWT Token..." spellcheck="false">'+tok+'</textarea><div>'+outHtml+'</div></div></div>';document.getElementById('jwt-sample').onclick=function(){tok=SAMPLE;decode(tok);render();};document.getElementById('jwt-clr').onclick=function(){tok='';result=null;err='';render();};document.getElementById('jwt-in').oninput=function(){tok=this.value;clearTimeout(timer);if(tok.split('.').length===3)timer=setTimeout(function(){decode(tok);render();},500);else{result=null;err='';}};}render();
```

---

## 4. number-converter

```
var r=document.getElementById('tool-root');var base='10',val='';var s=document.createElement('style');s.textContent='#nc{max-width:800px;margin:0 auto;padding:12px;font-family:system-ui,sans-serif}#nc *{box-sizing:border-box}.nc-bar{display:flex;justify-content:space-between;background:#1e293b;padding:3px 6px;border-radius:6px;margin-bottom:8px}.nc-tab{padding:4px 10px;border:none;border-radius:4px;cursor:pointer;font-size:.8rem;background:transparent;color:#94a3b8}.nc-tab.on{background:#3b82f6;color:#fff}.nc-areas{display:grid;grid-template-columns:1fr 1fr;gap:8px}.nc-in{width:100%;height:120px;padding:8px;font-family:monospace;font-size:.8rem;background:#0f172a;color:#e2e8f0;border:1px solid #334155;border-radius:4px;resize:vertical}.nc-out{background:#0f172a;border:1px solid #334155;border-radius:4px;padding:10px}.nc-row{display:flex;align-items:center;padding:6px 0;border-bottom:1px solid #1e293b}.nc-row:last-child{border-bottom:none}.nc-lbl{min-width:70px;font-size:.75rem;color:#64748b}.nc-val{font-family:monospace;font-size:.8rem;color:#e2e8f0;word-break:break-all}@media(max-width:600px){.nc-areas{grid-template-columns:1fr}}';document.head.appendChild(s);var SAMPLES={'2':'1010110','8':'777','10':'255','16':'FF'};var PH={'2':'输入二进制 (例如: 1010)','8':'输入八进制 (例如: 755)','10':'输入十进制 (例如: 123)','16':'输入十六进制 (例如: FF)'};function parse(v){v=v.trim();if(v.startsWith('0x')||v.startsWith('0X'))v=v.slice(2);else if(v.startsWith('0b')||v.startsWith('0B'))v=v.slice(2);else if(v.startsWith('0o')||v.startsWith('0O'))v=v.slice(2);return parseInt(v,parseInt(base));}function render(){var dec=val.trim()?parse(val):NaN;var bin=isNaN(dec)?'-':dec.toString(2);var oct=isNaN(dec)?'-':dec.toString(8);var d10=isNaN(dec)?'-':dec.toString(10);var hex=isNaN(dec)?'-':dec.toString(16).toUpperCase();r.innerHTML='<div id="nc"><div class="nc-bar"><div>'+['2','8','10','16'].map(function(b){return'<button class="nc-tab'+(base===b?' on':'')+'" data-b="'+b+'">'+{'2':'二进制','8':'八进制','10':'十进制','16':'十六进制'}[b]+'</button>';}).join('')+'<button class="nc-tab" id="nc-sample">示例</button></div><button class="nc-tab" id="nc-clr">× 清空</button></div><div class="nc-areas"><textarea class="nc-in" id="nc-in" placeholder="'+PH[base]+'" spellcheck="false">'+val+'</textarea><div class="nc-out"><div class="nc-row"><span class="nc-lbl">二进制：</span><span class="nc-val">'+bin+'</span></div><div class="nc-row"><span class="nc-lbl">八进制：</span><span class="nc-val">'+oct+'</span></div><div class="nc-row"><span class="nc-lbl">十进制：</span><span class="nc-val">'+d10+'</span></div><div class="nc-row"><span class="nc-lbl">十六进制：</span><span class="nc-val">'+hex+'</span></div></div></div></div>';document.querySelectorAll('.nc-tab[data-b]').forEach(function(btn){btn.onclick=function(){base=this.dataset.b;render();};});document.getElementById('nc-sample').onclick=function(){val=SAMPLES[base];render();};document.getElementById('nc-clr').onclick=function(){val='';render();};document.getElementById('nc-in').oninput=function(){val=this.value;render();};}render();
```

---

## 5. unicode-converter

```
var r=document.getElementById('tool-root');var mode='encode',inp='',out='';var s=document.createElement('style');s.textContent='#uc{max-width:800px;margin:0 auto;padding:12px;font-family:system-ui,sans-serif}#uc *{box-sizing:border-box}.uc-bar{display:flex;justify-content:space-between;background:#1e293b;padding:3px 6px;border-radius:6px;margin-bottom:8px}.uc-tab{padding:4px 10px;border:none;border-radius:4px;cursor:pointer;font-size:.8rem;background:transparent;color:#94a3b8}.uc-tab.on{background:#3b82f6;color:#fff}.uc-areas{display:grid;grid-template-columns:1fr 1fr;gap:8px}.uc-areas textarea{width:100%;height:200px;padding:8px;font-family:monospace;font-size:.8rem;background:#0f172a;color:#e2e8f0;border:1px solid #334155;border-radius:4px;resize:vertical}@media(max-width:600px){.uc-areas{grid-template-columns:1fr}}';document.head.appendChild(s);function convert(t){if(!t.trim())return'';if(mode==='encode'){return t.split('').map(function(c){var code=c.charCodeAt(0);return code>127?'\\u'+code.toString(16).padStart(4,'0'):c;}).join('');}else{try{return t.replace(/\\u([0-9a-fA-F]{4})/g,function(_,h){return String.fromCharCode(parseInt(h,16));});}catch(e){return'解码失败：无效的Unicode字符串';}}}function render(){r.innerHTML='<div id="uc"><div class="uc-bar"><div><button class="uc-tab'+(mode==='encode'?' on':'')+'" id="uc-e">编码</button><button class="uc-tab'+(mode==='decode'?' on':'')+'" id="uc-d">解码</button></div><button class="uc-tab" id="uc-c">× 清空</button></div><div class="uc-areas"><textarea id="uc-i" placeholder="输入需要编码/解码的Unicode文本..." spellcheck="false">'+inp+'</textarea><textarea id="uc-o" placeholder="转换结果将显示在这里..." readonly spellcheck="false">'+out+'</textarea></div></div>';document.getElementById('uc-e').onclick=function(){mode='encode';out=convert(inp);render();};document.getElementById('uc-d').onclick=function(){mode='decode';out=convert(inp);render();};document.getElementById('uc-c').onclick=function(){inp='';out='';render();};document.getElementById('uc-i').oninput=function(){inp=this.value;out=convert(inp);document.getElementById('uc-o').value=out;};}render();
```

---

## 6. url-encoder

```
var r=document.getElementById('tool-root');var mode='encode',inp='',out='';var s=document.createElement('style');s.textContent='#ue{max-width:800px;margin:0 auto;padding:12px;font-family:system-ui,sans-serif}#ue *{box-sizing:border-box}.ue-bar{display:flex;justify-content:space-between;background:#1e293b;padding:3px 6px;border-radius:6px;margin-bottom:8px}.ue-tab{padding:4px 10px;border:none;border-radius:4px;cursor:pointer;font-size:.8rem;background:transparent;color:#94a3b8}.ue-tab.on{background:#3b82f6;color:#fff}.ue-areas{display:grid;grid-template-columns:1fr 1fr;gap:8px}.ue-areas textarea{width:100%;height:200px;padding:8px;font-family:monospace;font-size:.8rem;background:#0f172a;color:#e2e8f0;border:1px solid #334155;border-radius:4px;resize:vertical}@media(max-width:600px){.ue-areas{grid-template-columns:1fr}}';document.head.appendChild(s);function convert(t){if(!t.trim())return'';try{return mode==='encode'?encodeURIComponent(t):decodeURIComponent(t);}catch(e){return mode==='encode'?'编码失败':'解码失败：无效的编码字符串';}}function render(){r.innerHTML='<div id="ue"><div class="ue-bar"><div><button class="ue-tab'+(mode==='encode'?' on':'')+'" id="ue-e">编码</button><button class="ue-tab'+(mode==='decode'?' on':'')+'" id="ue-d">解码</button></div><button class="ue-tab" id="ue-c">× 清空</button></div><div class="ue-areas"><textarea id="ue-i" placeholder="输入需要编码/解码的URL文本..." spellcheck="false">'+inp+'</textarea><textarea id="ue-o" placeholder="转换结果将显示在这里..." readonly spellcheck="false">'+out+'</textarea></div></div>';document.getElementById('ue-e').onclick=function(){mode='encode';out=convert(inp);render();};document.getElementById('ue-d').onclick=function(){mode='decode';out=convert(inp);render();};document.getElementById('ue-c').onclick=function(){inp='';out='';render();};document.getElementById('ue-i').oninput=function(){inp=this.value;out=convert(inp);document.getElementById('ue-o').value=out;};}render();
```

---

# API工具 client_code（6个）

复制下面每个工具的代码块内容，粘贴到后台对应工具的 client_code 字段。
这些工具前端只负责发请求，真实逻辑都在后端 `/api/*` 接口里。

---

## 1. ip-location（IP归属地查询）

```
var r=document.getElementById('tool-root');var s=document.createElement('style');s.textContent='#ipq{max-width:900px;margin:0 auto;padding:12px;font-family:system-ui,sans-serif;color:#e2e8f0}#ipq *{box-sizing:border-box}#ipq .row{display:flex;gap:8px;flex-wrap:wrap;align-items:center}#ipq input{flex:1;min-width:240px;padding:8px 10px;font-family:monospace;font-size:13px;background:#0f172a;color:#e2e8f0;border:1px solid #334155;border-radius:6px}#ipq button{padding:8px 12px;border:1px solid #334155;border-radius:6px;background:#1e293b;color:#e2e8f0;cursor:pointer}#ipq button.primary{background:#3b82f6;border-color:#3b82f6}#ipq pre{margin-top:12px;padding:10px;background:#0b1220;border:1px solid #334155;border-radius:6px;white-space:pre-wrap;word-break:break-word;overflow:auto}#ipq .muted{color:#94a3b8;font-size:12px;margin-top:6px}';document.head.appendChild(s);r.innerHTML='<div id=\"ipq\"><div class=\"row\"><input id=\"ipq-in\" placeholder=\"IP / 域名 / URL（留空=auto）\" spellcheck=\"false\"><button id=\"ipq-auto\">auto</button><button class=\"primary\" id=\"ipq-go\">查询</button><button id=\"ipq-copy\">复制结果</button></div><div class=\"muted\">后端接口：/api/ip-location（支持 IP / 域名 / URL / auto）</div><pre id=\"ipq-out\">{}</pre></div>';var inp=document.getElementById('ipq-in');var out=document.getElementById('ipq-out');var btn=document.getElementById('ipq-go');var copy=document.getElementById('ipq-copy');document.getElementById('ipq-auto').onclick=function(){inp.value='auto';};function show(v){out.textContent=typeof v==='string'?v:JSON.stringify(v,null,2);}async function run(){btn.disabled=true;show('Loading...');try{var v=(inp.value||'').trim();var q=v?('ip='+encodeURIComponent(v)):'ip=auto';var res=await fetch('/api/ip-location?'+q,{headers:{accept:'application/json'}});var data=null;try{data=await res.json();}catch(e){}if(!res.ok)throw new Error((data&&data.error&&data.error.message)||('HTTP '+res.status));if(data&&data.ok===false)throw new Error((data.error&&data.error.message)||'Request failed');show(data);}catch(e){show({ok:false,error:String(e&&e.message||e)});}finally{btn.disabled=false;}}btn.onclick=function(){run();};inp.onkeydown=function(e){if(e.key==='Enter'){e.preventDefault();run();}};copy.onclick=function(){if(navigator.clipboard)navigator.clipboard.writeText(out.textContent||'');};
```

---

## 2. dns-query（DNS解析查询）

```
var r=document.getElementById('tool-root');var s=document.createElement('style');s.textContent='#dnsq{max-width:900px;margin:0 auto;padding:12px;font-family:system-ui,sans-serif;color:#e2e8f0}#dnsq *{box-sizing:border-box}#dnsq .row{display:flex;gap:8px;flex-wrap:wrap;align-items:center}#dnsq input,#dnsq select{padding:8px 10px;font-family:monospace;font-size:13px;background:#0f172a;color:#e2e8f0;border:1px solid #334155;border-radius:6px}#dnsq input{flex:1;min-width:240px}#dnsq button{padding:8px 12px;border:1px solid #334155;border-radius:6px;background:#1e293b;color:#e2e8f0;cursor:pointer}#dnsq button.primary{background:#3b82f6;border-color:#3b82f6}#dnsq pre{margin-top:12px;padding:10px;background:#0b1220;border:1px solid #334155;border-radius:6px;white-space:pre-wrap;word-break:break-word;overflow:auto}#dnsq .muted{color:#94a3b8;font-size:12px;margin-top:6px}';document.head.appendChild(s);r.innerHTML='<div id=\"dnsq\"><div class=\"row\"><input id=\"dnsq-in\" placeholder=\"example.com\" spellcheck=\"false\"><select id=\"dnsq-type\"><option>A</option><option>AAAA</option><option>CNAME</option><option>MX</option><option>TXT</option><option>NS</option><option>SOA</option><option>SRV</option><option>PTR</option></select><button class=\"primary\" id=\"dnsq-go\">查询</button><button id=\"dnsq-copy\">复制结果</button></div><div class=\"muted\">后端接口：/api/dns-query?domain=...&type=...</div><pre id=\"dnsq-out\">{}</pre></div>';var inp=document.getElementById('dnsq-in');var typ=document.getElementById('dnsq-type');var out=document.getElementById('dnsq-out');var btn=document.getElementById('dnsq-go');var copy=document.getElementById('dnsq-copy');function show(v){out.textContent=typeof v==='string'?v:JSON.stringify(v,null,2);}async function run(){btn.disabled=true;show('Loading...');try{var d=(inp.value||'').trim();if(!d)throw new Error('请输入域名');var t=(typ.value||'A').trim().toUpperCase();var res=await fetch('/api/dns-query?domain='+encodeURIComponent(d)+'&type='+encodeURIComponent(t),{headers:{accept:'application/json'}});var data=null;try{data=await res.json();}catch(e){}if(!res.ok)throw new Error((data&&data.error&&data.error.message)||('HTTP '+res.status));if(data&&data.ok===false)throw new Error((data.error&&data.error.message)||'Request failed');show(data);}catch(e){show({ok:false,error:String(e&&e.message||e)});}finally{btn.disabled=false;}}btn.onclick=function(){run();};inp.onkeydown=function(e){if(e.key==='Enter'){e.preventDefault();run();}};copy.onclick=function(){if(navigator.clipboard)navigator.clipboard.writeText(out.textContent||'');};
```

---

## 3. icp-query（ICP备案查询）

```
var r=document.getElementById('tool-root');var s=document.createElement('style');s.textContent='#icpq{max-width:900px;margin:0 auto;padding:12px;font-family:system-ui,sans-serif;color:#e2e8f0}#icpq *{box-sizing:border-box}#icpq .row{display:flex;gap:8px;flex-wrap:wrap;align-items:center}#icpq input{flex:1;min-width:260px;padding:8px 10px;font-family:monospace;font-size:13px;background:#0f172a;color:#e2e8f0;border:1px solid #334155;border-radius:6px}#icpq button{padding:8px 12px;border:1px solid #334155;border-radius:6px;background:#1e293b;color:#e2e8f0;cursor:pointer}#icpq button.primary{background:#3b82f6;border-color:#3b82f6}#icpq pre{margin-top:12px;padding:10px;background:#0b1220;border:1px solid #334155;border-radius:6px;white-space:pre-wrap;word-break:break-word;overflow:auto}#icpq .muted{color:#94a3b8;font-size:12px;margin-top:6px}';document.head.appendChild(s);r.innerHTML='<div id=\"icpq\"><div class=\"row\"><input id=\"icpq-in\" placeholder=\"baidu.com\" spellcheck=\"false\"><button class=\"primary\" id=\"icpq-go\">查询</button><button id=\"icpq-copy\">复制结果</button></div><div class=\"muted\">后端接口：/api/icp-query?domain=...</div><pre id=\"icpq-out\">{}</pre></div>';var inp=document.getElementById('icpq-in');var out=document.getElementById('icpq-out');var btn=document.getElementById('icpq-go');var copy=document.getElementById('icpq-copy');function show(v){out.textContent=typeof v==='string'?v:JSON.stringify(v,null,2);}async function run(){btn.disabled=true;show('Loading...');try{var d=(inp.value||'').trim();if(!d)throw new Error('请输入域名');var res=await fetch('/api/icp-query?domain='+encodeURIComponent(d),{headers:{accept:'application/json'}});var data=null;try{data=await res.json();}catch(e){}if(!res.ok)throw new Error((data&&data.error&&data.error.message)||('HTTP '+res.status));if(data&&data.ok===false)throw new Error((data.error&&data.error.message)||'Request failed');show(data);}catch(e){show({ok:false,error:String(e&&e.message||e)});}finally{btn.disabled=false;}}btn.onclick=function(){run();};inp.onkeydown=function(e){if(e.key==='Enter'){e.preventDefault();run();}};copy.onclick=function(){if(navigator.clipboard)navigator.clipboard.writeText(out.textContent||'');};
```

---

## 4. phone-location（手机号归属地）

```
var r=document.getElementById('tool-root');var s=document.createElement('style');s.textContent='#phq{max-width:900px;margin:0 auto;padding:12px;font-family:system-ui,sans-serif;color:#e2e8f0}#phq *{box-sizing:border-box}#phq .row{display:flex;gap:8px;flex-wrap:wrap;align-items:center}#phq input{flex:1;min-width:260px;padding:8px 10px;font-family:monospace;font-size:13px;background:#0f172a;color:#e2e8f0;border:1px solid #334155;border-radius:6px}#phq button{padding:8px 12px;border:1px solid #334155;border-radius:6px;background:#1e293b;color:#e2e8f0;cursor:pointer}#phq button.primary{background:#3b82f6;border-color:#3b82f6}#phq pre{margin-top:12px;padding:10px;background:#0b1220;border:1px solid #334155;border-radius:6px;white-space:pre-wrap;word-break:break-word;overflow:auto}#phq .muted{color:#94a3b8;font-size:12px;margin-top:6px}';document.head.appendChild(s);r.innerHTML='<div id=\"phq\"><div class=\"row\"><input id=\"phq-in\" placeholder=\"请输入手机号（支持含空格/横线/+86）\" spellcheck=\"false\"><button class=\"primary\" id=\"phq-go\">查询</button><button id=\"phq-copy\">复制结果</button></div><div class=\"muted\">后端接口：/api/phone-location?phone=...</div><pre id=\"phq-out\">{}</pre></div>';var inp=document.getElementById('phq-in');var out=document.getElementById('phq-out');var btn=document.getElementById('phq-go');var copy=document.getElementById('phq-copy');function show(v){out.textContent=typeof v==='string'?v:JSON.stringify(v,null,2);}async function run(){btn.disabled=true;show('Loading...');try{var p=(inp.value||'').trim();if(!p)throw new Error('请输入手机号');var res=await fetch('/api/phone-location?phone='+encodeURIComponent(p),{headers:{accept:'application/json'}});var data=null;try{data=await res.json();}catch(e){}if(!res.ok)throw new Error((data&&data.error&&data.error.message)||('HTTP '+res.status));if(data&&data.ok===false)throw new Error((data.error&&data.error.message)||'Request failed');show(data);}catch(e){show({ok:false,error:String(e&&e.message||e)});}finally{btn.disabled=false;}}btn.onclick=function(){run();};inp.onkeydown=function(e){if(e.key==='Enter'){e.preventDefault();run();}};copy.onclick=function(){if(navigator.clipboard)navigator.clipboard.writeText(out.textContent||'');};
```

---

## 5. ascii-art（ASCII艺术字）

```
var r=document.getElementById('tool-root');var s=document.createElement('style');s.textContent='#asq{max-width:900px;margin:0 auto;padding:12px;font-family:system-ui,sans-serif;color:#e2e8f0}#asq *{box-sizing:border-box}#asq .row{display:flex;gap:8px;flex-wrap:wrap;align-items:center}#asq input,#asq select{padding:8px 10px;font-family:monospace;font-size:13px;background:#0f172a;color:#e2e8f0;border:1px solid #334155;border-radius:6px}#asq input{flex:1;min-width:240px}#asq button{padding:8px 12px;border:1px solid #334155;border-radius:6px;background:#1e293b;color:#e2e8f0;cursor:pointer}#asq button.primary{background:#3b82f6;border-color:#3b82f6}#asq pre{margin-top:12px;padding:10px;background:#0b1220;border:1px solid #334155;border-radius:6px;white-space:pre;overflow:auto}#asq .muted{color:#94a3b8;font-size:12px;margin-top:6px}';document.head.appendChild(s);r.innerHTML='<div id=\"asq\"><div class=\"row\"><input id=\"asq-in\" placeholder=\"输入文本（<=200字）\" spellcheck=\"false\"><select id=\"asq-font\"><option>3D Diagonal</option><option>Standard</option><option>Slant</option><option>Big</option><option>Small</option></select><button class=\"primary\" id=\"asq-go\">生成</button><button id=\"asq-copy\">复制ASCII</button></div><div class=\"muted\">后端接口：/api/ascii-art?text=...&font=...</div><pre id=\"asq-out\"></pre><pre id=\"asq-raw\" style=\"margin-top:10px;white-space:pre-wrap;word-break:break-word\"></pre></div>';var inp=document.getElementById('asq-in');var font=document.getElementById('asq-font');var out=document.getElementById('asq-out');var raw=document.getElementById('asq-raw');var btn=document.getElementById('asq-go');var copy=document.getElementById('asq-copy');function pickAscii(o){if(!o||typeof o!=='object')return null;var keys=['ascii_art','ascii','result','data'];for(var i=0;i<keys.length;i++){var v=o[keys[i]];if(typeof v==='string')return v;if(v&&typeof v==='object'&&typeof v.ascii_art==='string')return v.ascii_art;}for(var k in o){var vv=o[k];if(typeof vv==='string'&&vv.indexOf('\\n')!==-1&&vv.length>10)return vv;}return null;}function showAscii(t){out.textContent=t||'';}function showRaw(v){raw.textContent=typeof v==='string'?v:JSON.stringify(v,null,2);}async function run(){btn.disabled=true;showAscii('');showRaw('Loading...');try{var t=(inp.value||'').trim();if(!t)throw new Error('请输入文本');var f=(font.value||'3D Diagonal').trim();var res=await fetch('/api/ascii-art?text='+encodeURIComponent(t)+'&font='+encodeURIComponent(f),{headers:{accept:'application/json'}});var data=null;try{data=await res.json();}catch(e){}if(!res.ok)throw new Error((data&&data.error&&data.error.message)||('HTTP '+res.status));if(data&&data.ok===false)throw new Error((data.error&&data.error.message)||'Request failed');var a=pickAscii(data&&data.raw?data.raw:null);showAscii(a||'（未识别到 ASCII 字段，请看 raw JSON）');showRaw(data);}catch(e){showAscii('');showRaw({ok:false,error:String(e&&e.message||e)});}finally{btn.disabled=false;}}btn.onclick=function(){run();};inp.onkeydown=function(e){if(e.key==='Enter'){e.preventDefault();run();}};copy.onclick=function(){if(navigator.clipboard)navigator.clipboard.writeText(out.textContent||'');};
```

---

## 6. json-format（JSON格式化）

```
var r=document.getElementById('tool-root');var s=document.createElement('style');s.textContent='#jsq{max-width:980px;margin:0 auto;padding:12px;font-family:system-ui,sans-serif;color:#e2e8f0}#jsq *{box-sizing:border-box}#jsq .bar{display:flex;gap:8px;flex-wrap:wrap;align-items:center;margin-bottom:10px}#jsq button{padding:8px 12px;border:1px solid #334155;border-radius:6px;background:#1e293b;color:#e2e8f0;cursor:pointer}#jsq button.primary{background:#3b82f6;border-color:#3b82f6}#jsq .grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}#jsq textarea{width:100%;min-height:260px;padding:10px;font-family:monospace;font-size:13px;background:#0f172a;color:#e2e8f0;border:1px solid #334155;border-radius:6px;resize:vertical}#jsq .muted{color:#94a3b8;font-size:12px;margin-top:6px}#jsq pre{margin-top:10px;padding:10px;background:#0b1220;border:1px solid #334155;border-radius:6px;white-space:pre-wrap;word-break:break-word;overflow:auto}@media(max-width:800px){#jsq .grid{grid-template-columns:1fr}}';document.head.appendChild(s);r.innerHTML='<div id=\"jsq\"><div class=\"bar\"><button class=\"primary\" id=\"jsq-go\">格式化</button><button id=\"jsq-copy\">复制输出</button><button id=\"jsq-clear\">清空</button></div><div class=\"grid\"><div><div class=\"muted\">输入（JSON 字符串）</div><textarea id=\"jsq-in\" placeholder=\"粘贴 JSON...\" spellcheck=\"false\"></textarea></div><div><div class=\"muted\">输出（后端 /api/json-format 返回）</div><textarea id=\"jsq-out\" readonly spellcheck=\"false\"></textarea></div></div><pre id=\"jsq-msg\"></pre><div class=\"muted\">后端接口：POST /api/json-format  { content: \"...\" }</div></div>';var inp=document.getElementById('jsq-in');var out=document.getElementById('jsq-out');var msg=document.getElementById('jsq-msg');var btn=document.getElementById('jsq-go');var copy=document.getElementById('jsq-copy');document.getElementById('jsq-clear').onclick=function(){inp.value='';out.value='';msg.textContent='';};function setMsg(v){msg.textContent=typeof v==='string'?v:JSON.stringify(v,null,2);}async function run(){btn.disabled=true;setMsg('Loading...');out.value='';try{var t=(inp.value||'').trim();if(!t)throw new Error('请输入 JSON');var res=await fetch('/api/json-format',{method:'POST',headers:{'content-type':'application/json',accept:'application/json'},body:JSON.stringify({content:t})});var data=null;try{data=await res.json();}catch(e){}if(!res.ok)throw new Error((data&&data.error&&data.error.message)||('HTTP '+res.status));if(data&&data.ok===false)throw new Error((data.error&&data.error.message)||'Request failed');out.value=String(data.formatted||'');setMsg(data.stats?('OK  inputChars='+data.stats.inputChars+' outputChars='+data.stats.outputChars):'OK');}catch(e){setMsg({ok:false,error:String(e&&e.message||e)});}finally{btn.disabled=false;}}btn.onclick=function(){run();};copy.onclick=function(){if(navigator.clipboard)navigator.clipboard.writeText(out.value||'');};
```
